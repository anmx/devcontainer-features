## just

Installs just, a command runner.

### Options

| Option | Description |
| ------ | ----------- |
| `version` | The version of just to install. Defaults to `latest`. |
| `installCompletions` | Whether to install shell completions. Defaults to `false`. |
